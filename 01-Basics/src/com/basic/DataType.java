package com.basic;

public class DataType {
    public static void main(String[] args) {

        //Why data types are important in Java?
        String answer = """
                Data types are important in Java because
                Java is a strictly typed language and we 
                must inform the compiler that what type of
                data we are working with.
                """;

        String dataTypes = """
                There are two types of data type in Java.
                Primitive data type and user defined data type.
                Primitive data types are in built data type.
                User defined data types are defined by user
                """;

        System.out.println(answer);
        System.out.println(dataTypes);
    }
}
